<nav class="bg-white shadow-md">
  <div class="container mx-auto px-4 py-3 flex justify-between items-center">
    <div class="flex items-center">
      <%= link_to 'Notre Site', root_path, class: 'text-xl font-bold text-gray-800' %>
    </div>
    <div class="hidden md:flex space-x-4">
      <%= link_to 'Annuaire', directory_index_path, class: 'text-gray-700 hover:text-gray-900' %>
      <%= link_to 'Projets', projects_path, class: 'text-gray-700 hover:text-gray-900' %>
      <% if user_signed_in? %>
        <%= link_to 'Mon compte', edit_user_path(current_user), class: 'text-gray-700 hover:text-gray-900' %>
        <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: 'text-gray-700 hover:text-gray-900' %>
      <% else %>
        <%= link_to 'Connexion', new_user_session_path, class: 'text-gray-700 hover:text-gray-900' %>
        <%= link_to 'Inscription', new_user_registration_path, class: 'text-gray-700 hover:text-gray-900' %>
      <% end %>
    </div>
    <div class="md:hidden flex items-center">
      <button id="burger" class="focus:outline-none">
        <svg class="h-6 w-6 text-gray-800" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
          <path d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>
  </div>
  <div id="mobile-menu" class="md:hidden">
    <%= link_to 'Annuaire', directory_index_path, class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
    <%= link_to 'Projets', economic_projects_index_path, class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
    <% if user_signed_in? %>
      <%= link_to 'Mon compte', edit_user_path(current_user), class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
      <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
    <% else %>
      <%= link_to 'Connexion', new_user_session_path, class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
      <%= link_to 'Inscription', new_user_registration_path, class: 'block text-gray-700 hover:text-gray-900 px-4 py-2' %>
    <% end %>
  </div>
</nav>
